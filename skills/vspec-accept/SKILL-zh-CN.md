---
name: "vspec-accept"
description: "从规格生成场景驱动的验收测试用例（JSON）。当用户运行 /vspec:accept 时生成覆盖正常路径、异常、边界、权限和数据范围的验收用例。当用户需要将场景和规格转化为可执行的验收测试时，使用此 Skill。"
---

# vspec accept Skill（中文）

本 Skill 生成测试用例定义。可执行测试自动化代码由 vspec-script（Playwright）和 vspec-append-test 处理。

> 完整的命令流程请参见 `SKILL.md`（英文版）。
