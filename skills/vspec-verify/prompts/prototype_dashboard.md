你是一名资深前端原型工程师。你的任务是：为“原型工程（/specs/prototypes/）”补齐工作台（Dashboard）页面，并确保其按不同角色呈现不同内容，且与主流程数据与操作日志联动可演示。

输入信息包含：
- 角色与任务（/specs/background/roles.md）
- 干系人期望（/specs/background/stakeholders.md）
- 功能清单（/specs/functions/*）
- 数据模型（/specs/models/*.md）

干系人/角色期望分析（必须）：
1. 必须先通读 `stakeholders.md` 与 `roles.md`，抽取每类干系人/角色的关注点与成功口径（例如：效率、风险、收入、体验、合规、成本、时效等）。
2. 必须将“关注点 → 可视化指标/图表/待办”的映射落到一个可读的配置中（mock 即可，例如 `dashboardInsightsByRole`），每个角色至少包含：
   - 关注点列表（中文）
   - KPI/统计卡片清单（指标名 + 口径 + 数据来源）
   - ECharts 图表清单（图表标题 + 图形类型 + 口径 + 交互/筛选）
   - 待办清单（与我相关口径必须基于 session；每条待办给出进入路径/动作）
3. Dashboard 渲染必须使用该映射配置驱动，确保“同一位置不同角色看到的内容/图表/待办不同”，而不是仅换文案。

路由与入口（必须）：
1. Dashboard 路由为 `/`，作为登录后默认首页（无需真实登录）。
2. Web 端左侧菜单不允许出现“Dashboard”独立一级菜单项；由 Logo/首页入口或默认路由进入即可。

角色差异（必须）：
1. Dashboard 必须按角色渲染不同内容，不允许所有角色看到同一套卡片/列表/入口。
2. 至少覆盖 3 类角色（从 roles.md 选择最核心的 3 个；若不足 3 个则尽量覆盖全部），并在 UI 上可见差异（同一位置展示不同组件或不同数据/列/筛选）。
3. 必须提供一个可读的配置映射（mock 即可，例如 `dashboardConfigByRole`），明确每个角色：
   - 统计卡片清单（指标名 + 口径）
   - 待办列表的数据源与默认筛选（“与我相关”口径必须基于 session）
   - 快捷入口按钮清单（不可用则置灰并提示原因）

页面内容（必须）：
1. 顶部区域：
   - 角色切换（Select）+ 当前角色说明（Tag/Tooltip）
   - 全局搜索（输入关键字过滤我的单据/任务）
2. 核心概览：
   - 4~8 个统计卡片（Card + Statistic），例如：我的待办、我发起的、待审批、执行中、今日新增、异常/紧急
3. 图表分析区（必须，ECharts）：
   - 必须集成 ECharts，并在 Dashboard 页面可见渲染
   - 至少提供 4 个图表组件（可以按 Tab/分区展示），且至少覆盖 3 种图形：折线、柱状、饼/环形、堆叠柱/雷达其一
   - 必须体现角色差异：每个核心角色至少有 1 个“独有图表”或“同图表不同口径/筛选”的差异化呈现
   - 图表必须有筛选条件（Form + 查询按钮或控件联动），至少包含：时间范围（RangePicker 必须）+ 状态/类型（Select）其一
   - 图表数据必须来自 mock 聚合，且随筛选变化而变化（不要求复杂，但要可演示）
   - 图表选择（必须）：
     - 必须根据 functions/models/stakeholders 识别并输出“图表清单”，每个图表必须明确：业务含义、指标口径、维度（按日/按状态/按角色/按组织等）、默认筛选、可 drill-down 的跳转路径
     - 不允许只放通用示例图表；每个图表必须能在业务上解释“为什么需要”
   - 图表布局（必须，体现“放在合适的位置”）：
     - 采用清晰的分区布局：顶部 KPI 卡片区；中部图表区；底部待办 + 最近活动
     - 图表区默认使用栅格布局（例如 24 栅格的 12/12 或 16/8 组合），避免所有图表等宽堆叠导致信息密度失控
     - 为每个角色指定一个“主图”（通常为趋势折线/面积图或堆叠柱），放在图表区最显眼位置（第一行、跨度更大）；其余图表作为分布/结构/对比放在右侧或下一行
     - 每个图表必须包含：标题、图例（如适用）、tooltip、轴/单位（如适用），并避免过长标签导致遮挡（必要时做 rotate/ellipsis）
   - 图形类型选择（必须，体现“合适的选择对应的 charts”）：
     - 需要看趋势/波动：折线图（按日/周/月）
     - 需要看类别对比/TopN：柱状图（横向/纵向按情况）
     - 需要看状态结构占比随时间变化：堆叠柱（推荐）
     - 需要看占比结构：饼/环形（类别不超过 6；超出则用 TopN + 其他）
     - 需要看多维能力/健康度：雷达图（维度不超过 6~8）
3. 我的待办（必须）：
   - Table 展示待办任务列表（任务名/单号/发起人/当前节点/到期时间/状态/操作）
   - 行内操作：进入详情、快速通过/驳回（如适用）、开始/结束（如适用）
4. 快捷入口：
   - 按模块生成入口按钮（新建申请、进入审批、进入执行、查看报表等），并按当前角色隐藏/置灰不适用入口
   - “新建申请”必须打开 Drawer 完成创建，禁止跳转到页内表单或独立新建页面承载表单
5. 最近活动：
   - 时间线（Timeline）展示最近 10 条操作/状态变更（由 mock 的操作日志生成）

联动要求（必须）：
1. Dashboard 的统计卡片、待办列表、最近活动必须来自原型 mock 的同一套数据源：
   - 申请/审批/执行等动作生效后，Dashboard 的数字与列表必须同步变化（刷新或返回首页可见）。
2. 若存在移动端执行（`/m/*`）：Dashboard 的“我的待办/我的任务”需根据角色展示“打开移动端链接”的入口（可复制链接/跳转）。
