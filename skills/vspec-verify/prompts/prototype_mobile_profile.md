你是一名资深前端原型工程师。你的任务是：在“原型工程（/specs/prototypes/）”中补齐 Mobile 端“我的/个人中心（Profile）”页面与相关交互，用于演示会话信息、个人资料展示/编辑、偏好设置与账号安全入口（可使用 mock，无需真实后端）。

路由（前缀必须为 `/m/*`，且必须稳定可访问）：
- 我的/个人中心：`/m/profile`（必须；从移动端“我的”入口进入）

页面结构（必须）：
1. 顶部用户信息区（Card/List）：
   - 头像、昵称/姓名、角色、组织
   - 联系方式（若模型命中 phone/email）
2. 分组列表（至少 4 组）：
   - 账户与资料：编辑资料、账号信息（只读）
   - 偏好设置：主题、语言、通知偏好
   - 安全：修改密码、退出登录
   - 我的记录：我的操作日志/我的消息（占位也可但必须可访问）

交互与规则（必须可演示）：
1. 编辑资料：
   - 使用 Drawer/全屏页面编辑昵称/头像/联系方式（按需求命中裁剪）
   - 保存后写回 mock session/user 数据源，并在顶部信息区即时生效
2. 偏好设置：
   - 主题切换（浅色/深色，影响全局）
   - 语言切换：可选项来自 `/scheme.yaml` `selected.languages`
3. 安全：
   - 修改密码：跳转 `/m/change-password`（若不存在必须提示并禁止点击）
   - 退出登录：清空 session mock 并跳转 `/m/login`
4. 我的操作日志：
   - 列表页（List）展示：时间、操作类型、结果
   - 支持时间范围筛选（移动端筛选器页面/底部弹层）

会话与路由拦截（必须）：
- 未登录访问 `/m/profile` 必须引导到 `/m/login`

交互与控件规范（必须）：
- 移动端选择类控件必须使用 picker 页面或底部弹层（禁止下拉 Select）
- 所有操作必须有明确的成功/失败提示（中文文案）
